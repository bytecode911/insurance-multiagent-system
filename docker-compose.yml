version: '3.8'
services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
  orchestrator:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379
      - COMPLIANCE_STANDARD=irdai
      - NODE_ENV=production
    depends_on:
      - redis
